# Single-app 8 MB layout: 4 MB for the sketch, the rest for SPIFFS.
#
# The stock table for this board is Arduino's default_8MB.csv — a dual-OTA
# table that spends 3.1 MB on a second app slot a camera project almost never
# uses. This one keeps the same app size on an 8 MB and a 16 MB module, so
# raising board_upload.flash_size does not move any offsets.
#
# Do NOT switch to huge_app.csv here: that is a 4 MB table and its coredump
# partition lands at 0x3F0000, wasting the upper half of the flash.
# Name,     Type, SubType,  Offset,   Size,      Flags
nvs,        data, nvs,      0x9000,   0x5000,
phy_init,   data, phy,      0xE000,   0x1000,
factory,    app,  factory,  0x10000,  0x400000,
storage,    data, spiffs,   0x410000, 0x3E0000,
coredump,   data, coredump, 0x7F0000, 0x10000,
